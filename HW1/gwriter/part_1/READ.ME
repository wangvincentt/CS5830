1. python gwriter.py will generate test-ciphertext.txt by using plaintext.txt as input, and generate test-plaintext.txt by using ciphertext.txt as input. 
2. The encryption algorithm is strictly followed on instruction, while the decryption algorithm is reversely doing the what encryption does.  